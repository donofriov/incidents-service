apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "incidents-service.fullname" . }}-test-connection"
  labels:
    {{- include "incidents-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "incidents-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
